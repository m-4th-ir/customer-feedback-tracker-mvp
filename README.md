# Customer Feedback Tracker MVP

This repository contains the Customer Feedback Tracker MVP, including a FastAPI backend and (to be added) a React + Vite frontend.

## Backend

- FastAPI application under `backend/app`
- In-memory store for feedback records with seed data
- Dockerfile and Railway configuration for deployment

API base path: `/api/v1`

Health check: `GET /health`
